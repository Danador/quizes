import { reactive } from 'vue';

export const dataTypeCards = reactive({
    ratio: 'vertical',
    text: 'left',
    format: 'grid',
    answers: [
        {
            id: 1,
        },
        {
            id: 2,
        },
    ],
});
